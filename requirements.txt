# Cap below 2.0: mcp 2.0.0 (2026-07-28) removed mcp.server.fastmcp, which
# server.py imports. Keep in lockstep with mcpb/pyproject.toml and
# mcpb/remote-requirements.txt.
mcp[cli]>=1.0.0,<2.0.0
httpx>=0.27.0
pydantic>=2.0.0
cryptography>=41.0.0
# Desktop reports: real multi-sheet, formatted .xlsx export (pure-Python, no native deps).
openpyxl>=3.1.0
