// HeyCatch product analytics — auto-loaded by Next.js (>=15.3) at the client entry
// point. Module scope, static import, key inlined as a literal (safe to publish),
// init is idempotent. Autocapture handles clicks + pageviews; do not hand-instrument
// UI events. See https://heycatch.ai/agents.md.
import { analytics } from "@heycatch/sdk";

analytics.init({
  projectKey: "hck_pk_yZv02tLucLv76PiVL0gxfa7U8pqka-hp",
  install: {
    framework: "nextjs",
    frameworkVersion: "15",
    agent: "claude-code",
  },
});
