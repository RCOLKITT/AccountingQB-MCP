[package]
name = "accountingqb"
version = "0.1.0"
description = "AccountingQB — talk to your QuickBooks locally; your books stay on your machine"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
