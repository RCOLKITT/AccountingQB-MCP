# The shim reuses the connector's dependencies (root requirements.txt) plus a tiny
# web layer. Install both:
#   pip install -r requirements.txt -r accountingqb-local/requirements.txt
starlette>=0.37
uvicorn>=0.30
httpx>=0.27
