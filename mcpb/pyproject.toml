[project]
name = "accountingqb"
version = "3.17.6"
description = "AccountingQB — 101 AI tools connecting Claude to QuickBooks Online (US + Canada tax support)"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [
    { name = "AccountingQB", email = "support@accountingqb.com" }
]
keywords = ["mcp", "quickbooks", "accounting", "claude", "ai", "bookkeeping"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "License :: Other/Proprietary License",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial :: Accounting",
]
dependencies = [
    # Cap below 2.0: mcp 2.0.0 removed mcp.server.fastmcp (imported by server.py).
    "mcp[cli]>=1.0.0,<2.0.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
    "cryptography>=41.0.0",
]

[project.urls]
Homepage = "https://accountingqb.com"
Documentation = "https://accountingqb.com/docs"
Repository = "https://github.com/RCOLKITT/AccountingQB-MCP"

[project.scripts]
accountingqb = "accountingqb.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/accountingqb"]
